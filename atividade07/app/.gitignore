#!/bin/sh
# Module-level .gitignore for atividade07/app
# Root .gitignore already covers general build and IDE files.
# Add app-specific ignores here if necessary (eg. generated resources).

# Example:
# /build/